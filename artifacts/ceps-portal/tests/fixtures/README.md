# Synthetic Alta browser fixtures

These files were created solely for automated tests. Every person, UCI, authorization,
check, invoice, amount, and future-dated transaction is fictional.

Do not replace these fixtures with exports from Alta or with reports containing real
participant data. The browser test reads only the two explicitly named synthetic files.